// Generated from gradle/tasks/generate/FastRenderingBinding.java.template by the sibling
// stamp-fast-rendering-version.gradle. Do not edit: it is rewritten from the jar the adapter was
// type-checked against.
package kmlib.opengl;

/**
 * Which Fast Rendering release KMLib's bridge adapter was compiled against.
 *
 * <p>Runtime can read only the version installed now, so the version compiled against has to be
 * carried forward from the build or a mismatch report can name just one side of it.
 */
final class FastRenderingBinding {

    private static final String BOUND_VERSION = "{{boundVersion}}";

    // What a build stamps where it read no version: one compiled against the bridge stubs, or
    // against a jar too old to carry a version class. A word rather than an empty string so the
    // generated file, and a decompiled jar, say what happened. It never reaches a sentence: the
    // reader below answers it as no version.
    private static final String UNKNOWN_VERSION = "{{unknownVersion}}";

    private FastRenderingBinding() {
    }

    /**
     * Reports the release this jar's bridge adapter was type-checked against, or {@code null} where
     * the build read none.
     *
     * <p>A method rather than the constant because the sentinel is a fact about the stamp, not a
     * version; answering {@code null} for it is what keeps that word out of every reader.
     *
     * @return genir's own version string, or {@code null}
     */
    static String readBoundVersion() {
        return UNKNOWN_VERSION.equals(BOUND_VERSION) ? null : BOUND_VERSION;
    }
}
